# API base URL the admin server proxies to. Use loopback in dev (admin and api
# run on the same laptop). Override in prod.
API_BASE_URL=http://localhost:3000

# Optional — overrides the cookie name. Defaults to "trustnear_admin_session".
# SESSION_COOKIE_NAME=trustnear_admin_session
